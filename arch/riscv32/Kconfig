menu "BootConfig"
	config STACK_LEN
	int "stack length"
	default 4096

	config BOOT_START_ADDR
	hex "boot start address"
	default 0x00000000

	config BOOT_LEN
	hex "boot length"
	default 0x2000
endmenu
